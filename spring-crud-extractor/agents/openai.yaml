interface:
  display_name: "Spring SQL CRUD 提取器"
  short_description: "给定代码仓和 YAML 接口清单，离线提取 Java Spring 的 SQL CRUD 操作并生成 YAML 报告"
  default_prompt: "使用 $spring-crud-extractor 分析这个 Java Spring 代码仓和 YAML 接口清单，提取每个接口背后的 SQL CRUD 操作并生成 YAML 报告。"
